-- MySQL Script generated by MySQL Workbench
-- Tue Jul 21 14:02:19 2026
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

-- -----------------------------------------------------
-- Schema mydb
-- -----------------------------------------------------
-- -----------------------------------------------------
-- Schema textilera
-- -----------------------------------------------------

-- -----------------------------------------------------
-- Schema textilera
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `textilera` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci ;
USE `textilera` ;

-- -----------------------------------------------------
-- Table `textilera`.`conjunto`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`conjunto` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `nombre` VARCHAR(45) NOT NULL,
  `piezas` INT NOT NULL,
  `precio` DECIMAL(10,2) NOT NULL,
  `minimo` DECIMAL(10,2) NULL DEFAULT NULL,
  PRIMARY KEY (`id`))
ENGINE = InnoDB
AUTO_INCREMENT = 2
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`venta`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`venta` (
  `folio` INT NOT NULL AUTO_INCREMENT,
  `fecha` DATE NULL DEFAULT (curdate()),
  PRIMARY KEY (`folio`))
ENGINE = InnoDB
AUTO_INCREMENT = 3
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`detalleventaconjunto`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`detalleventaconjunto` (
  `folioVenta` INT NOT NULL,
  `idConjunto` INT NOT NULL,
  `cantidad` INT NULL DEFAULT NULL,
  `total` DECIMAL(10,2) NULL DEFAULT NULL,
  PRIMARY KEY (`folioVenta`, `idConjunto`),
  INDEX `conjDetaVen_idx` (`idConjunto` ASC) VISIBLE,
  CONSTRAINT `conjDetaVen`
    FOREIGN KEY (`idConjunto`)
    REFERENCES `textilera`.`conjunto` (`id`),
  CONSTRAINT `detaVenC`
    FOREIGN KEY (`folioVenta`)
    REFERENCES `textilera`.`venta` (`folio`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`tienda`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`tienda` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `tipo` VARCHAR(45) NOT NULL,
  `nombre` VARCHAR(45) NOT NULL,
  `idPadre` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  INDEX `tiendaPadre_idx` (`idPadre` ASC) VISIBLE,
  CONSTRAINT `tiendaPadre`
    FOREIGN KEY (`idPadre`)
    REFERENCES `textilera`.`tienda` (`id`))
ENGINE = InnoDB
AUTO_INCREMENT = 2
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`prenda`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`prenda` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `nombre` VARCHAR(45) NOT NULL,
  `talla` VARCHAR(45) NOT NULL,
  `existencia` INT NOT NULL,
  `precioMayoreo` DECIMAL(10,2) NOT NULL,
  `precioMenudeo` DECIMAL(10,2) NOT NULL,
  `idTienda` INT NULL DEFAULT NULL,
  `codigoBarras` VARCHAR(45) NULL DEFAULT NULL,
  `minimo` DECIMAL(10,2) NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `nombre` (`nombre` ASC, `talla` ASC) VISIBLE,
  UNIQUE INDEX `codigoBarras` (`codigoBarras` ASC) VISIBLE,
  INDEX `tiendaPren_idx` (`idTienda` ASC) VISIBLE,
  CONSTRAINT `prendaTienda`
    FOREIGN KEY (`idTienda`)
    REFERENCES `textilera`.`tienda` (`id`))
ENGINE = InnoDB
AUTO_INCREMENT = 8
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`detalleventaprenda`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`detalleventaprenda` (
  `folio` INT NOT NULL,
  `idPrenda` INT NOT NULL,
  `cantidad` INT NULL DEFAULT NULL,
  `total` DECIMAL(10,2) NULL DEFAULT NULL,
  PRIMARY KEY (`folio`, `idPrenda`),
  INDEX `prendaDetalle_idx` (`idPrenda` ASC) VISIBLE,
  CONSTRAINT `detaVenP`
    FOREIGN KEY (`folio`)
    REFERENCES `textilera`.`venta` (`folio`),
  CONSTRAINT `prendaDetalle`
    FOREIGN KEY (`idPrenda`)
    REFERENCES `textilera`.`prenda` (`id`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`devolucionconjunto`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`devolucionconjunto` (
  `folio` INT NOT NULL,
  `idConjunto` INT NOT NULL,
  `fecha` DATE NULL DEFAULT (curdate()),
  PRIMARY KEY (`folio`, `idConjunto`),
  INDEX `ventaDetalleCon_idx` (`folio` ASC) VISIBLE,
  INDEX `conjuntoDevConj_idx` (`idConjunto` ASC) VISIBLE,
  CONSTRAINT `conjuntoDevConj`
    FOREIGN KEY (`idConjunto`)
    REFERENCES `textilera`.`conjunto` (`id`),
  CONSTRAINT `devoC`
    FOREIGN KEY (`folio`)
    REFERENCES `textilera`.`venta` (`folio`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`devolucionprenda`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`devolucionprenda` (
  `folioVenta` INT NOT NULL,
  `idPrenda` INT NOT NULL,
  `fecha` DATE NULL DEFAULT (curdate()),
  PRIMARY KEY (`folioVenta`, `idPrenda`),
  INDEX `ventaDevolucion_idx` (`folioVenta` ASC) VISIBLE,
  INDEX `prendaDevolucion_idx` (`idPrenda` ASC) VISIBLE,
  CONSTRAINT `devoP`
    FOREIGN KEY (`folioVenta`)
    REFERENCES `textilera`.`venta` (`folio`),
  CONSTRAINT `prendaDevolucion`
    FOREIGN KEY (`idPrenda`)
    REFERENCES `textilera`.`prenda` (`id`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`ubicacion`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`ubicacion` (
  `id` INT NOT NULL,
  `tipo` VARCHAR(45) NOT NULL,
  `nombre` VARCHAR(45) NOT NULL,
  `idPadre` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  INDEX `padreUbicacion_idx` (`idPadre` ASC) VISIBLE,
  CONSTRAINT `padreUbicacion`
    FOREIGN KEY (`idPadre`)
    REFERENCES `textilera`.`ubicacion` (`id`)
    ON DELETE CASCADE
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`insumo`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`insumo` (
  `id` VARCHAR(45) NOT NULL,
  `numeroPartida` VARCHAR(45) NULL DEFAULT NULL,
  `existencia` DECIMAL(10,2) NOT NULL,
  `tipoExistencia` VARCHAR(45) NOT NULL,
  `descripcion` VARCHAR(45) NOT NULL,
  `nombre` VARCHAR(45) NOT NULL,
  `color` VARCHAR(45) NULL DEFAULT NULL,
  `medida` DECIMAL(10,2) NULL DEFAULT NULL,
  `ancho` DECIMAL(10,2) NULL DEFAULT NULL,
  `composicion` VARCHAR(45) NULL DEFAULT NULL,
  `tipo` VARCHAR(45) NULL DEFAULT NULL,
  `no.` INT NULL DEFAULT NULL,
  `tamanio` VARCHAR(45) NULL DEFAULT NULL,
  `talla` DECIMAL(10,2) NULL DEFAULT NULL,
  `material` VARCHAR(45) NULL DEFAULT NULL,
  `tipoInsumo` VARCHAR(45) NOT NULL,
  `idUbicacion` INT NULL DEFAULT NULL,
  `minimo` DECIMAL(10,2) NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `numeroPartida_UNIQUE` (`numeroPartida` ASC) VISIBLE,
  INDEX `ubicacionInsumo_idx` (`idUbicacion` ASC) VISIBLE,
  CONSTRAINT `ubicacionInsumo`
    FOREIGN KEY (`idUbicacion`)
    REFERENCES `textilera`.`ubicacion` (`id`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`insumoprenda`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`insumoprenda` (
  `folio` INT NOT NULL AUTO_INCREMENT,
  `idInsumo` VARCHAR(45) NULL DEFAULT NULL,
  `idPrenda` INT NULL DEFAULT NULL,
  `fecha` DATE NULL DEFAULT (curdate()),
  `cantidadInsumo` DECIMAL(10,2) NOT NULL,
  PRIMARY KEY (`folio`),
  INDEX `insumoIP_idx` (`idInsumo` ASC) VISIBLE,
  INDEX `prendaIP_idx` (`idPrenda` ASC) VISIBLE,
  CONSTRAINT `fk_insumoIP`
    FOREIGN KEY (`idInsumo`)
    REFERENCES `textilera`.`insumo` (`id`)
    ON DELETE CASCADE
    ON UPDATE CASCADE,
  CONSTRAINT `prendaIP`
    FOREIGN KEY (`idPrenda`)
    REFERENCES `textilera`.`prenda` (`id`)
    ON DELETE CASCADE
    ON UPDATE CASCADE)
ENGINE = InnoDB
AUTO_INCREMENT = 5
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`prendaconjunto`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`prendaconjunto` (
  `id` INT NOT NULL,
  `idPrenda` INT NULL DEFAULT NULL,
  `idConjunto` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  INDEX `prendaPC_idx` (`idPrenda` ASC) VISIBLE,
  INDEX `conjuntoPC_idx` (`idConjunto` ASC) VISIBLE,
  CONSTRAINT `conjuntoPC`
    FOREIGN KEY (`idConjunto`)
    REFERENCES `textilera`.`conjunto` (`id`)
    ON DELETE CASCADE
    ON UPDATE CASCADE,
  CONSTRAINT `prendaPC`
    FOREIGN KEY (`idPrenda`)
    REFERENCES `textilera`.`prenda` (`id`)
    ON DELETE CASCADE
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;


-- -----------------------------------------------------
-- Table `textilera`.`usuarios`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `textilera`.`usuarios` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `nombre` VARCHAR(100) NOT NULL,
  `usuario` VARCHAR(50) NOT NULL,
  `password` VARCHAR(255) NOT NULL,
  `rol` ENUM('administrador', 'encargado', 'vendedor') NOT NULL DEFAULT 'encargado',
  `email` VARCHAR(100) NULL DEFAULT NULL,
  `telefono` VARCHAR(20) NULL DEFAULT NULL,
  `activo` TINYINT(1) NULL DEFAULT '1',
  `fecha_creacion` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `fecha_actualizacion` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `usuario_UNIQUE` (`usuario` ASC) VISIBLE)
ENGINE = InnoDB
AUTO_INCREMENT = 4
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_0900_ai_ci;

USE `textilera`;

DELIMITER $$
USE `textilera`$$
CREATE
DEFINER=`root`@`localhost`
TRIGGER `textilera`.`devolucionVenta`
AFTER INSERT ON `textilera`.`devolucionconjunto`
FOR EACH ROW
begin
	update detalleventaConjunto set cantidad = 0.0 
    where folioVenta = new.folio and idConjunto = new.idConjunto;
end$$

USE `textilera`$$
CREATE
DEFINER=`root`@`localhost`
TRIGGER `textilera`.`devolucionVentaPrenda`
AFTER INSERT ON `textilera`.`devolucionprenda`
FOR EACH ROW
begin
	update detalleventaPrenda set cantidad = 0.0 
    where folio = new.folioVenta and idPrenda= new.idPrenda;
end$$

USE `textilera`$$
CREATE
DEFINER=`root`@`localhost`
TRIGGER `textilera`.`decrementoInsumo`
AFTER INSERT ON `textilera`.`insumoprenda`
FOR EACH ROW
begin
	update insumo set existencia = existencia-new.cantidadInsumo
    where id = new.idInsumo;
  end$$


DELIMITER ;

SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
